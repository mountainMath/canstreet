% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{collect_road_network}
\alias{collect_road_network}
\title{Materialize a road network query as an sf object}
\usage{
collect_road_network(x, crs = NULL)
}
\arguments{
\item{x}{A lazy table from \code{\link[=get_road_network]{get_road_network()}}.}

\item{crs}{Optional CRS to reproject to, as an EPSG code or authority
string. Defaults to the storage CRS, EPSG:3347.}
}
\value{
An \link[sf:sf]{sf::sf} object with \code{LINESTRING} geometry.
}
\description{
Reads a lazy table from \code{\link[=get_road_network]{get_road_network()}} into memory, converting the
geometry column to \pkg{sf}. Geometry crosses the boundary as WKB, which is
both faster and lossless compared with going through text.
}
\examples{
\dontrun{
get_road_network(1996) |>
  dplyr::filter(name == "WATER") |>
  collect_road_network()
}
}
