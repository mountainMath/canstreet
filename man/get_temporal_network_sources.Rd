% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tnet_api.R
\name{get_temporal_network_sources}
\alias{get_temporal_network_sources}
\title{The segment-to-source crosswalk for a build}
\usage{
get_temporal_network_sources(name, cache_path = canstreet_cache_path())
}
\arguments{
\item{name}{Build name.}

\item{cache_path}{Cache directory. Defaults to \code{\link[=canstreet_cache_path]{canstreet_cache_path()}}.}
}
\value{
A lazy \code{\link[dplyr:tbl]{dplyr::tbl()}}.
}
\description{
One row per segment per year it is present in, naming the arc of that year's
file it corresponds to. This is the matching itself, exposed: \code{match_kind}
says how the correspondence was established -- \code{"spine"} where the segment's
geometry came from that year, \code{"geometry"} or \code{"geometry+name"} where it was
found by proximity, and \code{"name_rescue"} where only the same-name rule at
longer range found it, which is the case for coarsely digitized long arcs.
\code{dist_m}, \code{name_match} and \code{bearing_diff} let a caller apply a stricter
standard than the build used.
}
\details{
\code{csduid} and \code{csdname} are that year's own values, not the newest year's, so
comparing them across years shows where census subdivision boundaries or
codes changed under a road that did not move.
}
\examples{
\dontrun{
library(dplyr)
get_temporal_network_sources("calgary") |>
  count(vintage, match_kind) |>
  collect()
}
}
