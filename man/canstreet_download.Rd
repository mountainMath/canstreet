% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{canstreet_download}
\alias{canstreet_download}
\title{Download the source archives for a road network vintage}
\usage{
canstreet_download(
  vintage,
  refresh = FALSE,
  quiet = FALSE,
  cache_path = canstreet_cache_path()
)
}
\arguments{
\item{vintage}{Reference year of the network, e.g. \code{2021}. See
\code{\link[=list_road_network_vintages]{list_road_network_vintages()}} for what is available.}

\item{refresh}{Re-download even when the archive is already cached.}

\item{quiet}{Suppress progress messages.}

\item{cache_path}{Cache directory. Defaults to \code{\link[=canstreet_cache_path]{canstreet_cache_path()}},
which reads the \code{CANSTREET_CACHE_PATH} environment variable, then the
\code{canstreet.cache_path} option, then falls back to \code{\link[=tempdir]{tempdir()}}. Set it once
with \code{\link[=set_canstreet_cache_path]{set_canstreet_cache_path()}} to keep data between sessions.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per downloaded archive:
\code{vintage}, \code{filename}, \code{path}, \code{url} and \code{label} (the area the archive
covers, for vintages that ship as per-area units).
}
\description{
Fetches the raw Statistics Canada archives for one vintage into the cache
and returns their paths. \code{\link[=get_road_network]{get_road_network()}} calls this for you; it is
exported so that a large download can be done deliberately, for instance to
warm a cache before working offline.
}
\details{
The archives are kept after import (see \code{\link[=list_canstreet_cache]{list_canstreet_cache()}}), so the
database can always be rebuilt without re-downloading several gigabytes.

Data are downloaded from Statistics Canada and from the Abacus Data Network
at the University of British Columbia, and are distributed under the
Statistics Canada Open Licence
(\url{https://www.statcan.gc.ca/en/reference/licence}).
}
\examples{
\dontrun{
# ~310 MB
canstreet_download(2021)
}
}
