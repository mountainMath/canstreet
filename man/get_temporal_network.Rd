% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tnet_api.R
\name{get_temporal_network}
\alias{get_temporal_network}
\title{Access a temporal road network build}
\usage{
get_temporal_network(name, within = NULL, cache_path = canstreet_cache_path())
}
\arguments{
\item{name}{Build name passed to \code{\link[=build_temporal_network]{build_temporal_network()}}.}

\item{within}{Optional spatial filter: an \pkg{sf} or \code{sfc} object, or a
\code{bbox}. Uses the table's R-tree index.}

\item{cache_path}{Cache directory. Defaults to \code{\link[=canstreet_cache_path]{canstreet_cache_path()}}.}
}
\value{
A lazy \code{\link[dplyr:tbl]{dplyr::tbl()}} over the cached database.
}
\description{
Returns the harmonized table as a lazy \pkg{dbplyr} table. Nothing is read
into memory until you call \code{\link[=collect_road_network]{collect_road_network()}}, so an aggregation over
a national build runs inside the database.
}
\details{
Columns: \code{segment_id}; \code{spine_vintage}, \code{spine_id}, \code{spine_piece},
\code{spine_lo} and \code{spine_hi}, which say exactly which arc of which year's file
the geometry was cut from and where; \code{years}, a list column of the vintages
the segment is present in, with \code{year_key}, \code{n_years}, \code{first_year} and
\code{last_year} derived from it; the harmonized attribute columns, carried from
the spine arc; \code{len_m}; and \code{geom} in EPSG:3347.

\code{year_key} is the same information as \code{years} written as \code{"1996|2006|2021"}.
Group by it to total road length by presence pattern; use \code{years} with
\code{list_contains()} to ask whether a particular year is in the set.
}
\examples{
\dontrun{
get_temporal_network("calgary") |>
  dplyr::filter(n_years == 1, first_year == 2021) |>
  collect_road_network()
}
}
