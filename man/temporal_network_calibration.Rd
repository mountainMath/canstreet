% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tnet_api.R
\name{temporal_network_calibration}
\alias{temporal_network_calibration}
\title{How the matching tolerance was chosen}
\usage{
temporal_network_calibration(name, cache_path = canstreet_cache_path())
}
\arguments{
\item{name}{Build name.}

\item{cache_path}{Cache directory. Defaults to \code{\link[=canstreet_cache_path]{canstreet_cache_path()}}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}}, one row per calibrated vintage. Zero rows if
the build used a supplied \code{tolerance} instead of calibrating.
}
\description{
Reports what the build measured before it matched anything, so the tolerance
can be judged rather than taken on trust. For each vintage against the
newest one in the build:
}
\details{
\describe{
\item{\code{recall_p50}, \code{recall_p90}}{How far an arc's midpoint sits from the
nearest arc of the newest vintage carrying the same name. This is the
positional disagreement between the two files.}
\item{\code{disagree_10}, \code{disagree_25}, \code{disagree_40}}{The share of arcs whose
\emph{nearest} arc within that many metres carries a different name -- the
rate at which the tolerance starts matching a road to its neighbour.}
\item{\code{dx}, \code{dy}}{Mean displacement. Near zero means the disagreement is
noise; a systematic offset would mean the two files are registered
differently, which this package does not correct for and warns about.}
}
}
\examples{
\dontrun{
temporal_network_calibration("calgary")
}
}
