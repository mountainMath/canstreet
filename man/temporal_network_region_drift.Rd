% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tnet_api.R
\name{temporal_network_region_drift}
\alias{temporal_network_region_drift}
\title{Where census subdivision boundaries moved under a road that did not}
\usage{
temporal_network_region_drift(name, cache_path = canstreet_cache_path())
}
\arguments{
\item{name}{Build name.}

\item{cache_path}{Cache directory. Defaults to \code{\link[=canstreet_cache_path]{canstreet_cache_path()}}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with \code{vintage_from}, \code{vintage_to},
\code{csduid_from}, \code{csdname_from}, \code{csduid_to}, \code{csdname_to}, \code{n_segments} and
\code{km}, ordered by \code{km}. Zero rows if no vintage pair in the build carries
the attribute, or if nothing changed.
}
\description{
Statistics Canada reuses census subdivision codes across boundary revisions,
so the same \code{CSDUID} in two census years is not necessarily the same ground.
This is why \code{\link[=build_temporal_network]{build_temporal_network()}} restricts to a region by stamping one
fixed polygon across every year rather than by filtering on the attribute.
}
\details{
Because the crosswalk records each vintage's own \code{csduid_l} against a segment
whose geometry is fixed, the drift becomes directly measurable: this function
reports, for each consecutive pair of vintages in the build, every change of
census subdivision along road that is present in both years. A road cannot
move between municipalities on its own, so every row is a boundary revision,
an annexation, or a recoding.

Only vintages that carry the attribute can contribute. The 1996 Street
Network File and the 2006 Road Network File have no region columns at all,
which is the other half of the reason the spatial stamp is not optional.
}
\examples{
\dontrun{
temporal_network_region_drift("calgary")
}
}
