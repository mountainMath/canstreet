% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache_mgmt.R
\name{list_canstreet_cache}
\alias{list_canstreet_cache}
\title{Inspect the canstreet cache}
\usage{
list_canstreet_cache(cache_path = canstreet_cache_path())
}
\arguments{
\item{cache_path}{Cache directory. Defaults to \code{\link[=canstreet_cache_path]{canstreet_cache_path()}},
which reads the \code{CANSTREET_CACHE_PATH} environment variable, then the
\code{canstreet.cache_path} option, then falls back to \code{\link[=tempdir]{tempdir()}}. Set it once
with \code{\link[=set_canstreet_cache_path]{set_canstreet_cache_path()}} to keep data between sessions.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per imported vintage --
\code{vintage}, \code{n_segments}, \code{n_archives}, \code{raw_mb}, \code{imported_at},
\code{package_version} -- carrying the size of the database file in a
\code{db_mb} attribute and, when any temporal network has been built, the
result of \code{\link[=list_temporal_networks]{list_temporal_networks()}} in a \code{builds} attribute. Zero rows if
nothing has been imported.
}
\description{
Reports what the cache holds: which vintages are imported, how many segments
each contributed, and how much disk the downloaded archives and the database
occupy.
}
\examples{
list_canstreet_cache()
}
