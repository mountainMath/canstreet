% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{get_road_network}
\alias{get_road_network}
\title{Access a road network vintage}
\usage{
get_road_network(
  vintage,
  within = NULL,
  refresh = FALSE,
  quiet = FALSE,
  cache_path = canstreet_cache_path()
)
}
\arguments{
\item{vintage}{Reference year, or a vector of years. See
\code{\link[=list_road_network_vintages]{list_road_network_vintages()}}.}

\item{within}{Optional spatial filter: an \pkg{sf} or \code{sfc} object, or a
\code{bbox}. Segments intersecting it are returned. Reprojected to the storage
CRS for you. A filter over a single vintage uses the R-tree index.}

\item{refresh}{Re-download and re-import even if the vintage is cached.}

\item{quiet}{Suppress progress messages.}

\item{cache_path}{Cache directory. Defaults to \code{\link[=canstreet_cache_path]{canstreet_cache_path()}},
which reads the \code{CANSTREET_CACHE_PATH} environment variable, then the
\code{canstreet.cache_path} option, then falls back to \code{\link[=tempdir]{tempdir()}}. Set it once
with \code{\link[=set_canstreet_cache_path]{set_canstreet_cache_path()}} to keep data between sessions.}
}
\value{
A lazy \code{\link[dplyr:tbl]{dplyr::tbl()}} over the cached database.
}
\description{
Downloads and imports the vintage on first use, then returns it as a lazy
\pkg{dbplyr} table backed by the cached 'DuckDB' store. Nothing is read into
memory until you call \code{\link[=collect_road_network]{collect_road_network()}} (or \code{\link[dplyr:collect]{dplyr::collect()}} on a
selection without the geometry column), so a filter or an aggregation runs
inside the database over all 2.2 million segments of a national vintage.
}
\details{
Passing more than one vintage returns them stacked, which is the starting
point for comparing the network across years.

Segments are harmonized into a single schema; see \code{\link[=canstreet_schema]{canstreet_schema()}}. All
geometry is stored in EPSG:3347 (NAD83 / Statistics Canada Lambert), so
\code{len_m} and any distance computed from \code{geom} are in metres regardless of
the vintage's own coordinate system.

Source data are © Statistics Canada, distributed under the Statistics Canada
Open Licence (\url{https://www.statcan.gc.ca/en/reference/licence}). Cite the
product and reference year in anything you publish from it.
}
\examples{
\dontrun{
library(dplyr)

# First call downloads ~310 MB and imports it; later calls are instant.
roads <- get_road_network(2021)

# Aggregation runs in the database, not in R.
roads |>
  group_by(pruid_l) |>
  summarize(km = sum(len_m, na.rm = TRUE) / 1000) |>
  collect()

# Spatial filter, materialized as sf.
bbox <- sf::st_bbox(c(xmin = -123.2, ymin = 49.2, xmax = -123.0,
                      ymax = 49.3), crs = 4326)
vancouver <- get_road_network(2021, within = bbox) |>
  collect_road_network()
}
}
\seealso{
\code{\link[=collect_road_network]{collect_road_network()}} to materialize the result as \pkg{sf},
\code{\link[=export_road_network]{export_road_network()}} to write GeoParquet.
}
