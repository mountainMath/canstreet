% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache_path.R
\name{set_canstreet_cache_path}
\alias{set_canstreet_cache_path}
\title{Set the canstreet cache location}
\usage{
set_canstreet_cache_path(cache_path, overwrite = FALSE, install = FALSE)
}
\arguments{
\item{cache_path}{Directory to cache data in, e.g. \code{"~/data/canstreet"}. It
is created if it does not exist.}

\item{overwrite}{Replace a \code{CANSTREET_CACHE_PATH} already in \code{.Renviron}.}

\item{install}{Write the setting to \code{.Renviron} so it persists across
sessions. When \code{FALSE} it applies to this session only.}
}
\value{
\code{cache_path}, invisibly.
}
\description{
Points canstreet at a directory to keep downloaded archives and the
harmonized database in. With \code{install = TRUE} the setting is written to your
\code{.Renviron} so it applies to every future session, which is what you want:
without it, several hundred megabytes per vintage are re-downloaded each
time you restart R.
}
\examples{
\dontrun{
# This session only.
set_canstreet_cache_path("~/data/canstreet")

# Permanently.
set_canstreet_cache_path("~/data/canstreet", install = TRUE)
}
}
