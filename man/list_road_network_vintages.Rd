% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{list_road_network_vintages}
\alias{list_road_network_vintages}
\title{List the available road network vintages}
\usage{
list_road_network_vintages(cache_path = canstreet_cache_path())
}
\arguments{
\item{cache_path}{Cache directory. Defaults to \code{\link[=canstreet_cache_path]{canstreet_cache_path()}},
which reads the \code{CANSTREET_CACHE_PATH} environment variable, then the
\code{canstreet.cache_path} option, then falls back to \code{\link[=tempdir]{tempdir()}}. Set it once
with \code{\link[=set_canstreet_cache_path]{set_canstreet_cache_path()}} to keep data between sessions.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per vintage: \code{vintage}, \code{product},
\code{catalogue}, \code{host}, \code{coverage}, \code{source_crs}, \code{imported}, \code{n_segments}
and \code{notes}.
}
\description{
Reports every vintage this package knows how to fetch, what it covers, and
-- when a cache is present -- whether it has already been imported.
}
\examples{
list_road_network_vintages()
}
